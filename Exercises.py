class Employee:
    
    def__init__(self, first, last, title, salary):
        self.first_name = first
        self.last_name = last
        self.job.title = title
        self.salary = salary
        self. email = first[0].lower()+last.lower()+last.lower()
        
    def apply.raise(self)
        self.salary +- Employee Raise AMount 

